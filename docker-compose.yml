version: '3.8'

services:
  # Backend Service (Flask)
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: em-backend
    ports:
      - "5000:5000"
    env_file:
      - ./backend/.env
    environment:
      - FLASK_ENV=production
    networks:
      - em-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5000/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  # Frontend Service (Next.js)
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: em-frontend
    ports:
      - "3001:3000"
    environment:
      - NEXT_PUBLIC_API_URL=http://localhost:5000
    depends_on:
      - backend
    networks:
      - em-network
    restart: unless-stopped

  # Prometheus Service
  prometheus:
    image: prom/prometheus
    container_name: em-prometheus
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml
    ports:
      - "9090:9090"
    networks:
      - em-network
    restart: unless-stopped

  # Grafana Service
  grafana:
    image: grafana/grafana
    container_name: em-grafana
    ports:
      - "3002:3000"
    depends_on:
      - prometheus
    networks:
      - em-network
    restart: unless-stopped

networks:
  em-network:
    driver: bridge
